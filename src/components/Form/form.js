/** @jsx jsx */
import { jsx, Box, Input, Button } from "theme-ui"

const Form = () => {
  return (
    <Box as="form">
      <Input />
      <Input />
      <Button>CONTACT US</Button>
    </Box>
  )
}

export default Form
